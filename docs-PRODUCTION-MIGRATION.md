# Production Migration (Compatibility Pointer)

The maintained migration document now lives at `docs/PRODUCTION-MIGRATION.md`.

This compatibility file remains so existing links do not break. New documentation should link to the `docs/` path.
